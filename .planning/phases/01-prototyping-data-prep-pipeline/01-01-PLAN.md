---
wave: 1
depends_on: []
files_modified: ["source/Data_Preparation_Prototype.ipynb", ".env.example", "requirements.txt"]
autonomous: true
---

# Plan: 01-01 - Prototyping OCR extraction and pdf2image loading

## Objective
Set up the Jupyter Notebook environment to correctly load a tiny sample of Thai election PDFs using `pdf2image`, then perform a side-by-side extraction using Typhoon OCR (via API) and PaddleOCR (local execution).

## Tasks

<task>
<action>
Create `.env.example` in the project root with the following keys for future usage:
`TYPHOON_API_KEY=`
</action>
<read_first>
- .planning/phases/01-prototyping-data-prep-pipeline/01-CONTEXT.md
</read_first>
<acceptance_criteria>
- `.env.example` exists and contains exactly `TYPHOON_API_KEY=`
</acceptance_criteria>
</task>

<task>
<action>
Create `requirements.txt` with the key libraries: `pdf2image`, `paddlepaddle`, `paddleocr`, `python-dotenv`, `requests`, `pandas`, `jupyterlab`.
Do not include version locks.
</action>
<read_first>
- .planning/phases/01-prototyping-data-prep-pipeline/01-RESEARCH.md
</read_first>
<acceptance_criteria>
- `requirements.txt` exists and contains `pdf2image`, `paddlepaddle`, `paddleocr`, `python-dotenv`, `requests`, `pandas`, `jupyterlab`.
</acceptance_criteria>
</task>

<task>
<action>
Create a new Jupyter Notebook `source/Data_Preparation_Prototype.ipynb`.
In the first cell, add standard imports and load the `.env` file using `dotenv`.
In the next cell, write a function `convert_pdf_to_images(pdf_path)` using `pdf2image.convert_from_path` to load a sample PDF file from `source/เขตเลือกตั้งที่ 2/ล่วงหน้าในเขต/อำเภอบ้านไร่/ตำบลคอกควาย/หน่วยเลือกตั้งที่ 1/`.
In the next cell, set up basic functions to query Typhoon OCR (using `requests` to an imagined API endpoint sending base64 or multipart images) and local PaddleOCR (initializing `PaddleOCR(use_angle_cls=True, lang='th')`).
</action>
<read_first>
- .planning/phases/01-prototyping-data-prep-pipeline/01-CONTEXT.md
- .planning/phases/01-prototyping-data-prep-pipeline/01-RESEARCH.md
</read_first>
<acceptance_criteria>
- `source/Data_Preparation_Prototype.ipynb` is a valid notebook JSON file.
- Contains imports for `pdf2image`, `os`, `dotenv`, `requests`, `paddleocr`.
- Contains `convert_pdf_to_images` function that utilizes `pdf2image`.
</acceptance_criteria>
</task>

## Verification
- Run `cat .env.example` to ensure it exists and matches the required keys.
- Check `jupyter nbconvert --to script source/Data_Preparation_Prototype.ipynb` output to ensure valid python code syntax.

## Must Haves
- The notebook structure must support side-by-side OCR comparison for the user.
- Environment keys must be handled correctly via `.env`.

## Requirements Addressed
- EXTR-01
- EXTR-02
- EXTR-03
- EXTR-04
